## **Yet Another URL Shortener**

It's a simple Python powered Flask app, that makes a tiny URL's instead of users big URL and still direct to a required page.
YAUS use a base64 encode to generate unic tiny url.
